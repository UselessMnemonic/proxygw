services:
  workspace:
    build:
      context: ..
      dockerfile: Dockerfile
      target: devcontainer
    command: sleep infinity
    working_dir: /workspaces/proxy-gateway
    cap_add:
      - NET_ADMIN
